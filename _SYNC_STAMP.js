/**
 * ============================================================
 *  SYNC STAMP — ตัวยืนยันเวอร์ชัน (Version Verification Stamp)
 * ============================================================
 * ไฟล์นี้อยู่ทั้งบน Git และ GAS พร้อมกัน
 * วิธีใช้ยืนยันว่า "โค้ดที่อัพโหลด = เวอร์ชันล่าสุดที่ตกลงกันในแชทจริง":
 *   1. เปิดไฟล์นี้บน GAS (Apps Script editor) แล้วดู topic + updated
 *   2. เทียบกับไฟล์เดียวกันบน GitHub และกับหัวข้อที่คุยกันในแชท
 *   3. ถ้า topic + updated ตรงกันทั้ง 3 ที่ = เป็นเวอร์ชันเดียวกันแน่นอน
 *
 * อัพเดทโดย Claude ทุกครั้งที่มีการเปลี่ยนแปลงโค้ดที่ตกลงกัน
 * (แก้ค่าใน SYNC_STAMP() ด้านล่าง)
 */
function SYNC_STAMP() {
  return {
    topic:   'ระบบ auto-sync + verify: .claspignore, git-hygiene reminder, stamp ยืนยันเวอร์ชัน',
    updated: '2026-07-11 15:55 +07:00',
    branch:  'main',
    based_on_commit: '01b4101',
    note:    'เวอร์ชันล่าสุดที่ตกลงกันในแชท — verify .claspignore แล้ว push 30 ไฟล์'
  };
}
